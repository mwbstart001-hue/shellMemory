# -n 和 -i 参数边界检查测试报告

## 测试日期
2026-04-30

## 测试分支
v11-qianshi

## 修复内容

### 1. 新增 `is_integer()` 函数
- 位置：`system_status.sh:489-491`
- 功能：只接受正整数（1, 2, 3...）
- 正则：`^[1-9][0-9]*$`

```bash
is_integer() {
    [[ "$1" =~ ^[1-9][0-9]*$ ]]
}
```

### 2. 修改 `is_number()` 函数
- 位置：`system_status.sh:485-487`
- 功能：拒绝前导零格式（如 `00.5`、`05`）
- 正则：`^-?(0|[1-9][0-9]*)(\.[0-9]+)?$`

```bash
is_number() {
    [[ "$1" =~ ^-?(0|[1-9][0-9]*)(\.[0-9]+)?$ ]]
}
```

### 3. 修改主命令 `-n` 参数检查
- 位置：`system_status.sh:376-381`
- 改动：使用 `is_integer()` 替代 `is_number()` + 整数比较
- 错误信息：`错误: 采样次数必须是正整数`

### 4. 添加主命令 `-i` 参数下限检查
- 位置：`system_status.sh:392-394`
- 改动：添加 `>= 0.1` 秒的下限检查
- 错误信息：`错误: 采样间隔必须大于等于0.1秒`

### 5. 修改 `query` 子命令 `-n` 参数检查
- 位置：
  - `system_status.sh:1099-1104` (parse_query_args)
  - `system_status.sh:1556-1562` (query_alarms)
- 错误信息：`错误: 查询数量必须是正整数`

### 6. 修改 `stats` 子命令 `-d` 参数检查
- 位置：`system_status.sh:1933-1939`
- 错误信息：`错误: 天数必须是正整数`

---

## 测试场景

### 第一部分：-n 参数（采样次数）测试

#### 1. 浮点数输入（应该拒绝）
| 测试用例 | 预期结果 | 实际结果 | 状态 |
|---------|---------|---------|------|
| `-n 1.5` | 错误: 采样次数必须是正整数 | 错误: 采样次数必须是正整数 | ✅ PASS |
| `-n 2.9` | 错误: 采样次数必须是正整数 | 错误: 采样次数必须是正整数 | ✅ PASS |
| `-n 0.5` | 错误: 采样次数必须是正整数 | 错误: 采样次数必须是正整数 | ✅ PASS |

#### 2. 异常数字格式（应该拒绝）
| 测试用例 | 预期结果 | 实际结果 | 状态 |
|---------|---------|---------|------|
| `-n .5` (以点开头) | 错误: 采样次数必须是正整数 | 错误: 采样次数必须是正整数 | ✅ PASS |
| `-n 5.` (以点结尾) | 错误: 采样次数必须是正整数 | 错误: 采样次数必须是正整数 | ✅ PASS |
| `-n 001` (前导零) | 错误: 采样次数必须是正整数 | 错误: 采样次数必须是正整数 | ✅ PASS |

#### 3. 零和负数（应该拒绝）
| 测试用例 | 预期结果 | 实际结果 | 状态 |
|---------|---------|---------|------|
| `-n 0` | 错误: 采样次数必须是正整数 | 错误: 采样次数必须是正整数 | ✅ PASS |
| `-n -1` | 错误: 采样次数必须是正整数 | 错误: 采样次数必须是正整数 | ✅ PASS |
| `-n -5` | 错误: 采样次数必须是正整数 | 错误: 采样次数必须是正整数 | ✅ PASS |

#### 4. 正常输入（应该接受）
| 测试用例 | 预期结果 | 实际结果 | 状态 |
|---------|---------|---------|------|
| `-n 1` | 正常执行 | 正常执行 | ✅ PASS |
| `-n 5` | 正常执行 | 正常执行 | ✅ PASS |
| `-n 100` | 正常执行 | 正常执行 | ✅ PASS |

---

### 第二部分：-i 参数（采样间隔）测试

#### 1. 极小值（应该拒绝，< 0.1）
| 测试用例 | 预期结果 | 实际结果 | 状态 |
|---------|---------|---------|------|
| `-i 0.0001` | 错误: 采样间隔必须大于等于0.1秒 | 错误: 采样间隔必须大于等于0.1秒 | ✅ PASS |
| `-i 0.01` | 错误: 采样间隔必须大于等于0.1秒 | 错误: 采样间隔必须大于等于0.1秒 | ✅ PASS |
| `-i 0.05` | 错误: 采样间隔必须大于等于0.1秒 | 错误: 采样间隔必须大于等于0.1秒 | ✅ PASS |

#### 2. 零和负数（应该拒绝）
| 测试用例 | 预期结果 | 实际结果 | 状态 |
|---------|---------|---------|------|
| `-i 0` | 错误: 采样间隔必须大于0 | 错误: 采样间隔必须大于0 | ✅ PASS |
| `-i 0.0` | 错误: 采样间隔必须大于0 | 错误: 采样间隔必须大于0 | ✅ PASS |
| `-i -1` | 错误: 采样间隔必须大于0 | 错误: 采样间隔必须大于0 | ✅ PASS |
| `-i -0.5` | 错误: 采样间隔必须大于0 | 错误: 采样间隔必须大于0 | ✅ PASS |

#### 3. 前导零格式（应该拒绝）
| 测试用例 | 预期结果 | 实际结果 | 状态 |
|---------|---------|---------|------|
| `-i 00.5` (前导零) | 错误: 采样间隔必须是有效的数字 | 错误: 采样间隔必须是有效的数字 | ✅ PASS |
| `-i 05.5` (前导零) | 错误: 采样间隔必须是有效的数字 | 错误: 采样间隔必须是有效的数字 | ✅ PASS |
| `-i 001.5` (前导零) | 错误: 采样间隔必须是有效的数字 | 错误: 采样间隔必须是有效的数字 | ✅ PASS |

#### 4. 异常数字格式（应该拒绝）
| 测试用例 | 预期结果 | 实际结果 | 状态 |
|---------|---------|---------|------|
| `-i .5` (以点开头) | 错误: 采样间隔必须是有效的数字 | 错误: 采样间隔必须是有效的数字 | ✅ PASS |
| `-i 5.` (以点结尾) | 错误: 采样间隔必须是有效的数字 | 错误: 采样间隔必须是有效的数字 | ✅ PASS |

#### 5. 正常输入（应该接受）
| 测试用例 | 预期结果 | 实际结果 | 状态 |
|---------|---------|---------|------|
| `-i 0.1` (刚好等于下限) | 正常执行 | 正常执行 | ✅ PASS |
| `-i 0.5` | 正常执行 | 正常执行 | ✅ PASS |
| `-i 1.0` | 正常执行 | 正常执行 | ✅ PASS |
| `-i 5.5` | 正常执行 | 正常执行 | ✅ PASS |
| `-i 0` (零但实际是整数0) | 错误: 采样间隔必须大于0 | 错误: 采样间隔必须大于0 | ✅ PASS |

---

### 第三部分：query 子命令 -n 参数测试

#### 1. 浮点数输入（应该拒绝）
| 测试用例 | 预期结果 | 实际结果 | 状态 |
|---------|---------|---------|------|
| `query -n 1.5` | 错误: 查询数量必须是正整数 | 错误: 查询数量必须是正整数 | ✅ PASS |
| `query -n 0.5` | 错误: 查询数量必须是正整数 | 错误: 查询数量必须是正整数 | ✅ PASS |

#### 2. 前导零（应该拒绝）
| 测试用例 | 预期结果 | 实际结果 | 状态 |
|---------|---------|---------|------|
| `query -n 001` | 错误: 查询数量必须是正整数 | 错误: 查询数量必须是正整数 | ✅ PASS |

#### 3. 零和负数（应该拒绝）
| 测试用例 | 预期结果 | 实际结果 | 状态 |
|---------|---------|---------|------|
| `query -n 0` | 错误: 查询数量必须是正整数 | 错误: 查询数量必须是正整数 | ✅ PASS |
| `query -n -1` | 错误: 查询数量必须是正整数 | 错误: 查询数量必须是正整数 | ✅ PASS |

#### 4. 正常输入（应该接受）
| 测试用例 | 预期结果 | 实际结果 | 状态 |
|---------|---------|---------|------|
| `query -n 1` | 正常执行 | 正常执行 | ✅ PASS |
| `query -n 10` | 正常执行 | 正常执行 | ✅ PASS |

---

### 第四部分：stats 子命令 -d 参数测试

#### 1. 浮点数输入（应该拒绝）
| 测试用例 | 预期结果 | 实际结果 | 状态 |
|---------|---------|---------|------|
| `stats -d 1.5` | 错误: 天数必须是正整数 | 错误: 天数必须是正整数 | ✅ PASS |
| `stats -d 0.5` | 错误: 天数必须是正整数 | 错误: 天数必须是正整数 | ✅ PASS |

#### 2. 前导零（应该拒绝）
| 测试用例 | 预期结果 | 实际结果 | 状态 |
|---------|---------|---------|------|
| `stats -d 007` | 错误: 天数必须是正整数 | 错误: 天数必须是正整数 | ✅ PASS |

#### 3. 零和负数（应该拒绝）
| 测试用例 | 预期结果 | 实际结果 | 状态 |
|---------|---------|---------|------|
| `stats -d 0` | 错误: 天数必须是正整数 | 错误: 天数必须是正整数 | ✅ PASS |
| `stats -d -1` | 错误: 天数必须是正整数 | 错误: 天数必须是正整数 | ✅ PASS |

#### 4. 正常输入（应该接受）
| 测试用例 | 预期结果 | 实际结果 | 状态 |
|---------|---------|---------|------|
| `stats -d 1` | 正常执行 | 正常执行 | ✅ PASS |
| `stats -d 30` | 正常执行 | 正常执行 | ✅ PASS |

---

## 测试结果汇总

| 类别 | 测试用例数 | 通过数 | 失败数 |
|------|-----------|--------|--------|
| -n 参数测试 | 12 | 12 | 0 |
| -i 参数测试 | 16 | 16 | 0 |
| query -n 参数测试 | 6 | 6 | 0 |
| stats -d 参数测试 | 6 | 6 | 0 |
| **总计** | **43** | **43** | **0** |

## 结论

✅ **所有 43 个测试用例全部通过！**

### 关键改进

1. **整数参数校验**：`-n`、`query -n`、`stats -d` 现在只接受正整数，拒绝浮点数、前导零、零和负数。

2. **浮点数参数校验**：`-i` 参数现在拒绝前导零格式（如 `00.5`），并设置最小采样间隔为 0.1 秒。

3. **错误信息统一**：整数参数拒绝时统一报错"必须是正整数"，比之前的"必须大于0"或"必须是有效的数字"更准确。

### 测试脚本

测试脚本位置：`test_n_i_boundary_cases.sh`

运行方式：
```bash
./test_n_i_boundary_cases.sh
```
